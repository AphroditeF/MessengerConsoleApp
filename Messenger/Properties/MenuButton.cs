using System;
namespace Messenger
{
    public class MenuButton
    {

        //It should have name, be object of a menu object and be pointer

        string Name;


        public MenuButton(string name)
        {
            Name = name;
        }
    }
}